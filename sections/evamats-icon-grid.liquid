{%- liquid
  assign item_count = 0
  for block in section.blocks
    if block.type == 'item'
      assign item_count = item_count | plus: 1
    endif
  endfor

  assign layout_style = section.settings.layout_style | default: 'grid'
  assign heading_alignment = section.settings.heading_alignment | default: 'center'
  assign section_classes = 'evamats-icon-grid'
  if layout_style == 'rows'
    assign section_classes = section_classes | append: ' evamats-icon-grid--rows'
  endif
  if heading_alignment == 'left'
    assign section_classes = section_classes | append: ' evamats-icon-grid--heading-left'
  endif
-%}

{% render 'evamats-section-padding-style', section: section, target: '.evamats-icon-grid' %}
<div
  id="EvamatsIconGrid-{{ section.id }}"
  class="{{ section_classes }}"
>
  <div class="evamats-icon-grid__inner page-width">
    {%- if section.settings.heading != blank -%}
      <h2 class="evamats-icon-grid__heading section_title">{{ section.settings.heading | escape }}</h2>
    {%- endif -%}

    {%- if item_count > 0 -%}
      <ul class="evamats-icon-grid__grid" role="list">
        {%- for block in section.blocks -%}
          {%- if block.type == 'item' -%}
            <li class="evamats-icon-grid__item" {{ block.shopify_attributes }}>
              <div class="evamats-icon-grid__icon" aria-hidden="true">
                {%- if block.settings.icon != blank -%}
                  {{
                    block.settings.icon
                    | image_url: width: 96
                    | image_tag: loading: 'lazy', class: 'evamats-icon-grid__icon-img', alt: ''
                  }}
                {%- endif -%}
              </div>

              <div class="evamats-icon-grid__body">
                {%- if block.settings.heading != blank -%}
                  <h3 class="evamats-icon-grid__title">{{ block.settings.heading | escape }}</h3>
                {%- endif -%}

                {%- if block.settings.text != blank -%}
                  <p class="evamats-icon-grid__text">{{ block.settings.text | escape }}</p>
                {%- endif -%}
              </div>
            </li>
          {%- endif -%}
        {%- endfor -%}
      </ul>
    {%- endif -%}
  </div>
</div>

{% comment %} styles moved to assets/base_v2.css (stylesheet) {% endcomment %}

{% schema %}
{
  "name": "Carvion — Icon grid",
  "tag": "section",
  "class": "section-evamats-icon-grid",
  "settings": [
    {
      "type": "select",
      "id": "layout_style",
      "label": "Layout",
      "options": [
        {
          "value": "grid",
          "label": "Grid (3 columns)"
        },
        {
          "value": "rows",
          "label": "Rows (vertical list)"
        }
      ],
      "default": "grid"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Section heading (optional)"
    },
    {
      "type": "select",
      "id": "heading_alignment",
      "label": "Heading alignment",
      "options": [
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "left",
          "label": "Left"
        }
      ],
      "default": "center"
    },
    {
      "type": "header",
      "content": "Section spacing (desktop)"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 80
    },
    {
      "type": "header",
      "content": "Section spacing (mobile)"
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "label": "Padding top",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 32
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "label": "Padding bottom",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 32
    }
  ],
  "blocks": [
    {
      "type": "item",
      "name": "Item",
      "settings": [
        {
          "type": "image_picker",
          "id": "icon",
          "label": "Icon",
          "info": "White glyph on transparent background, ~36×36 px (desktop)."
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Jasná očekávání"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Text",
          "default": "Víte, co od vás očekáváme a v čem spočívá vaše role."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Carvion — Icon grid",
      "category": "V2",
      "blocks": [
        {
          "type": "item",
          "settings": {
            "heading": "Jasná očekávání",
            "text": "Víte, co od vás očekáváme a v čem spočívá vaše role."
          }
        },
        {
          "type": "item",
          "settings": {
            "heading": "Podpora vedoucího",
            "text": "Máte na koho se obrátit s dotazem a můžete počítat s feedbackem."
          }
        },
        {
          "type": "item",
          "settings": {
            "heading": "Rozvoj v praxi",
            "text": "Učíte se na reálných úkolech, ne jen z teorie."
          }
        },
        {
          "type": "item",
          "settings": {
            "heading": "Vliv na práci",
            "text": "Vaše nápady mohou skutečně měnit způsob fungování týmu."
          }
        },
        {
          "type": "item",
          "settings": {
            "heading": "Stabilní prostředí",
            "text": "Pracujeme na reálném produktu a rozvíjíme firmu dlouhodobě."
          }
        },
        {
          "type": "item",
          "settings": {
            "heading": "Lidská atmosféra",
            "text": "Spojujeme odpovědnost za výsledek s normální komunikací a vzájemným respektem."
          }
        }
      ]
    }
  ]
}
{% endschema %}
