{%- liquid
  assign card_count = 0
  for block in section.blocks
    if block.type == 'card'
      assign card_count = card_count | plus: 1
    endif
  endfor

  assign cta_url = section.settings.cta_url
  if cta_url == blank
    assign cta_url = '#'
  endif

  assign hover_label = section.settings.card_cta_label
  if hover_label == blank
    assign hover_label = section.settings.mobile_cta_label
  endif
-%}

<div
  id="PartnersAudience-{{ section.id }}"
  class="partners-audience-v2"
  aria-label="{{ section.settings.heading | escape }}"
>
  <div class="partners-audience-v2__inner page-width">
    {%- if section.settings.heading != blank -%}
      <h2 class="partners-audience-v2__heading section_title">{{ section.settings.heading | escape }}</h2>
    {%- endif -%}

    {%- if card_count > 0 -%}
      <ul class="partners-audience-v2__grid" role="list">
        {%- for block in section.blocks -%}
          {%- if block.type == 'card' -%}
            <li class="partners-audience-v2__item" {{ block.shopify_attributes }}>
              <a
                class="partners-audience-v2__card"
                href="{{ cta_url }}"
                {% if section.settings.cta_url == blank %}
                  aria-disabled="true"
                  tabindex="-1"
                {% endif %}
              >
                {%- if block.settings.image != blank -%}
                  <div class="partners-audience-v2__media" aria-hidden="true">
                    {{
                      block.settings.image
                      | image_url: width: 800
                      | image_tag:
                        loading: 'lazy',
                        class: 'partners-audience-v2__media-img',
                        widths: '163, 305, 610, 800',
                        sizes: '(min-width: 750px) 305px, 163px'
                    }}
                  </div>
                {%- endif -%}

                <div class="partners-audience-v2__copy">
                  {%- if block.settings.title != blank -%}
                    <h3 class="partners-audience-v2__title">{{ block.settings.title | escape }}</h3>
                  {%- endif -%}

                  {%- if block.settings.text != blank -%}
                    <p class="partners-audience-v2__text">{{ block.settings.text | escape }}</p>
                  {%- endif -%}
                </div>

                {%- if hover_label != blank -%}
                  <span class="partners-audience-v2__card-cta button button_3" aria-hidden="true">
                    {{ hover_label | escape }}
                  </span>
                {%- endif -%}
              </a>
            </li>
          {%- endif -%}
        {%- endfor -%}
      </ul>
    {%- endif -%}

    {%- if section.settings.promo_title != blank or section.settings.promo_text != blank -%}
      <div class="partners-audience-v2__promo">
        {%- if section.settings.promo_title != blank -%}
          <p class="partners-audience-v2__promo-title">{{ section.settings.promo_title | escape }}</p>
        {%- endif -%}
        {%- if section.settings.promo_text != blank -%}
          <p class="partners-audience-v2__promo-text">{{ section.settings.promo_text | escape }}</p>
        {%- endif -%}
      </div>
    {%- endif -%}

    {%- if section.settings.mobile_cta_label != blank -%}
      <div class="partners-audience-v2__footer-cta">
        <a
          class="button button_3 partners-audience-v2__footer-btn"
          href="{{ cta_url }}"
          {% if section.settings.cta_url == blank %}
            aria-disabled="true"
            tabindex="-1"
          {% endif %}
        >
          {{ section.settings.mobile_cta_label | escape }}
        </a>
      </div>
    {%- endif -%}
  </div>
</div>

{% comment %} styles moved to assets/base.css (stylesheet) {% endcomment %}

{% schema %}
{
  "name": "Partners audience",
  "tag": "section",
  "class": "section-partners-audience-v2",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Pro koho je tato spolupráce?"
    },
    {
      "type": "header",
      "content": "Link i przyciski"
    },
    {
      "type": "url",
      "id": "cta_url",
      "label": "URL linku",
      "info": "Desktop: klik w kartę. Mobile: przycisk pod kartami. Ten sam adres."
    },
    {
      "type": "text",
      "id": "card_cta_label",
      "label": "Card hover button (desktop)",
      "default": "Odeslat dotaz"
    },
    {
      "type": "text",
      "id": "mobile_cta_label",
      "label": "Mobile button",
      "default": "Odeslat dotaz"
    },
    {
      "type": "header",
      "content": "Promo strip (below cards)"
    },
    {
      "type": "text",
      "id": "promo_title",
      "label": "Promo title",
      "default": "Chcete začít vlastní podnikání?"
    },
    {
      "type": "text",
      "id": "promo_text",
      "label": "Promo text",
      "default": "Začněte prodávat bez vlastní výroby"
    }
  ],
  "blocks": [
    {
      "type": "card",
      "name": "Audience card",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Card title"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Text",
          "default": "Card description."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Partners audience",
      "category": "V2",
      "blocks": [
        {
          "type": "card",
          "settings": {
            "title": "Ruční výroba",
            "text": "Vytváříte produkty s péčí o každý detail? Naše EVA koberce jsou ideálním doplněním vaší nabídky — snadno se přizpůsobí, jsou odolné a estetické."
          }
        },
        {
          "type": "card",
          "settings": {
            "title": "Dealerzy samochodowi",
            "text": "Zvyšte hodnotu každého auta při prodeji. EVA koberce jsou praktický doplněk, který udělá skvělý první dojem a zvýší atraktivitu nabídky."
          }
        },
        {
          "type": "card",
          "settings": {
            "title": "E-commerce",
            "text": "Hledáte produkt, který se dobře prodává online? EVA koberce jsou lehké, snadno se odesílají a jsou v mnoha variantách — ideální pro škálování prodeje."
          }
        },
        {
          "type": "card",
          "settings": {
            "title": "Studia detailingu",
            "text": "Zvyšte standard svých služeb. EVA koberce jsou perfektní dokončení po detailingu — čistota, odolnost a efekt „jako nové auto“."
          }
        }
      ]
    }
  ]
}
{% endschema %}
