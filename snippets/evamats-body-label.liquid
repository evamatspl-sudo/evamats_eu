{%- comment -%}
  Localized body type label backed by the body_types metaobject.
  Parameter: handle.
{%- endcomment -%}
{%- liquid
  assign body_object = shop.metaobjects.body_types[handle]
  assign body_label = body_object.title.value | default: handle
-%}
{{- body_label -}}
